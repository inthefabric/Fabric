using Fabric.Api.Server;
using Nancy;
using Nancy.Testing;
using NUnit.Framework;

namespace Fabric.Test.Integration {

	/*================================================================================================*/
	//[TestFixture]
	public class FirstTry {


		////////////////////////////////////////////////////////////////////////////////////////////////
		/*--------------------------------------------------------------------------------------------* /
		[Test]
		public void Attempt() {
			var api = typeof(ApiModule);
			var boot = new DefaultNancyBootstrapper();
			var browser = new Browser(boot);

			BrowserResponse result = browser.Get("/", b => b.HttpRequest());

			Assert.Fail(result.Body.AsString()+"");
		}*/

	}

}